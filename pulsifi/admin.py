"""
    Admin configurations for models in pulsifi app.
"""

# from django.contrib import admin

# from .models import Profile, Pulse


# TODO: add admin pages with multiple permission levels, hide first & last names from admin, disable user creation in admin (or test if creating sends signal)
